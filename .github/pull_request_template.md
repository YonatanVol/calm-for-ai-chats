## What changed, and why

<!-- The reason matters more than the diff. What was wrong before? -->

## How it was verified

- [ ] `node tools/harness.js` passes
- [ ] `python3 install.py --zip` passes
- [ ] New behaviour has a scenario written **before** the fix, seen to fail
- [ ] The fix was mutated to confirm the test actually catches it
- [ ] Anything visual was **rendered and looked at**, in both themes

<!-- If a check does not apply, say so rather than ticking it. A green that
     was not earned is worse than a blank box. -->

## Anything you got wrong on the way

<!-- Optional, and genuinely useful. False positives and wrong first attempts
     cost the next person real time when they are not written down. -->
